import { ValidiFiBankLogin } from "./index";

describe("validi-fi-bank-login-typescript-sdk", () => {
    it("initialize client", async () => {
        const validifibanklogin = new ValidiFiBankLogin({
        });
    });
});
